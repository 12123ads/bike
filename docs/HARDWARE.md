# 电瓶车定位 + 手机开锁：硬件与决策

> 这份是给你看的：**买什么、怎么接、哪些坑会烧板子、你还需要拍板什么**。
> 写代码需要的东西（引脚映射、AT 命令、协议报文、MQTT 契约）全在
> [`DESIGN.md`](DESIGN.md) 里，这里不重复。
> 两份文档冲突时以 `DESIGN.md` 为准，并且告诉我，我来改这一份。

## 1. 这台设备做什么

装在电瓶车上，从 48V 车电池取电（不装自己的电池）：

- **定位**：GPS/北斗定位，走 4G 上报到家里的服务器，在 Home Assistant 里能看到车在哪。
- **开锁**：手机贴一下车就开锁。**不用蓝牙、不用实体卡、只支持安卓**。
- **防盗感知**：车被动了会触发上报（靠加速度计），平时睡着省电。

---

## 2. 采购清单

### 2.1 现在就能买

| 项 | 型号 / 规格 | 数量 | 备注 |
| --- | --- | --- | --- |
| 主控板 | nRF52840 ProMicro / nice!nano 克隆板 | 1 | 别买模组版，要裸片版才有 NFC 引脚 |
| 降压模块 | **LX-P160**，28~130V → 5V | 1 | 你已经选定 |
| 4G 模组 | **Air780EP** | 1 | 买回来要刷 AT 固件 |
| GNSS 模块 | **ATGM336H-5N** | 1 | 支持北斗，别买 NEO-7M |
| 加速度计 | **LIS2DW12** 模块 | 1 | 运动唤醒用 |
| TVS 管 | **SMBJ58A** | 1 | 防浪涌 |
| 保险丝 + 座 | 250~500 mA，**直流耐压 ≥100V** | 1 | 见 §3 第 1 条，别买 250V AC 的 |
| 电阻 | 1 MΩ × 2、4.7 kΩ × 2 | 各 2 | 电压采样 + I2C 上拉 |
| 小信号 N-MOS | 任意常见型号（2N7002 一类） | 1 | 给电压采样做门控 |
| 电容 | 按 LX-P160 手册配 | 若干 | 大电解会漏电，见 §3 第 6 条 |
| **机械钥匙 / 应急锁孔** | — | 1 | **不可省，见 §6** |
| 非金属外壳 | 塑料 / ABS | 1 | 金属壳会挡住 NFC 和 GPS 信号 |
| SIM 卡 + 流量套餐 | 物联网卡即可 | 1 | 用量极小，选最便宜的档 |
| 4G 天线 | IPEX 接口 | 1 | 模组只带座子，天线要单买 |
| GNSS 天线 | 有源或无源 | 1 | 装的位置要能看到天 |
| 接线端子 / 线束 | — | 若干 | 从电池组接出来那一段 |

### 2.2 先别买：型号还没定

这几项要等设计细节定了或者你先做选择，买早了会白买：

| 项 | 卡在哪 |
| --- | --- |
| **锁执行机构** | 你先定用电磁锁还是电机锁（见 §7 第 1 条），驱动电路跟着它选 |
| 锁驱动电路 | 同上 |
| 锁位置反馈微动开关 | 跟着锁的机械结构选 |
| **Q1 P 沟道 MOSFET + 栅源齐纳** | 防反接用，要耐压 ≥100V，型号待定 |
| GNSS 电源门控开关管 | 型号待定 |
| NFC 环形天线 + 2 颗调谐电容 | 电容容值必须实测，见 §3 第 5 条 |

### 2.3 工具

| 项 | 干什么 | 必须吗 |
| --- | --- | --- |
| J-Link 或 CMSIS-DAP | 烧录、解锁芯片 | **必须** |
| 万用表（能测 µA） | 测静态电流 | **必须**，见 §3 第 2 条 |
| 示波器 | 测 5V 有没有过冲（这个会烧芯片） | **必须** |
| 杜邦线、排针、烙铁 | 接线 | 必须 |
| VNA | 调 NFC 天线 | 可选，没有就用示波器凑 |

### 2.4 明确不要买

| 项 | 为什么 |
| --- | --- |
| NFC 读卡器芯片（FM17622 等）、实体 NFC 卡 | 改成手机当读卡器了，这颗芯片整个不需要 |
| 备份锂电池、LM66100、NTC | 你决定不做备份电源 |
| 串联肖特基二极管 | 改用 P-MOS，肖特基漏电太大 |
| LM5164 及其外围元件 | 改用你选的 LX-P160 成品模块 |
| 硬件欠压保护电路 | 改成软件判断 |
| 震动开关模块（LM393 那种） | 耗电 2~4 mA，比整机预算大 1000 倍 |

---

## 3. 装的时候必须注意的 6 件事

这 6 条每一条都能让你白花钱或者烧掉板子。

**1. 保险丝要看直流耐压，不是交流。**
48V 电池充满是 58.8V。常见的「250V」保险丝标的是交流，直流分断能力远低于这个数。
必须选明确标注 **直流 ≥100V** 的型号。

**2. 板子到手第一件事是测静态电流，别急着焊。**
这批克隆板质量是抽奖：同款板子实测过 0.42 µA、7~8 µA、750 µA 三种结果，
差了 1700 倍。如果拿到的是 750 µA 那种，整个省电设计要重做——早测比晚测好。

**3. 5V 只能灌 VBAT 焊盘。**
板子上有 VBUS、VCC、EXTVCC、VBAT 几个看起来都像电源的焊盘：

- 灌 **VBAT** ← 唯一正确
- 灌 VBUS → 会让板上的充电芯片在没有电池的回路里一直工作
- 灌 VCC / EXTVCC → **什么都没供上**，那是个输出脚，你在往一个稳压器的输出口倒电

**4. 5V 只剩 0.5V 余量，必须用示波器确认。**
芯片的绝对上限是 5.5V。LX-P160 输出 5V，只剩 0.5V 空间给它的精度误差和上电瞬间的过冲。
**上电瞬态超过 5.5V 就是永久损坏**，装机后先用示波器看一眼再接芯片。

**5. NFC 天线必须自己调，不是插上就能用。**
两颗调谐电容的容值取决于你最后用的那个天线的电感量，方案里给不出定值。
要么用 VNA 调，要么示波器凑。这一步跳不过去。

**6. 大电解电容自己会漏电。**
按「容量 × 电压 × 0.01」估漏流。在这个设计里，一颗选大了的电解电容
漏掉的电可以和整机待机耗电一样多。滤波电容别贪大。

### 接线总览

```
48V 电池 PACK+
   │
   ├─[保险丝 250~500mA，直流耐压≥100V]
   ├─[Q1 P-MOS 防反接]           ← 不要用肖特基二极管
   ├─[SMBJ58A TVS 到 PACK-]
   └─[LX-P160 模块] ──5V──→ 主控板 VBAT 焊盘   ← 只能是 VBAT
                                  │
        ┌─────────────────────────┼──────────────────────┐
        │              │          │           │          │
   LIS2DW12       ATGM336H    Air780EP      锁驱动    NFC 天线
   (加速度计)      (GPS)       (4G)                  (13.56MHz 环形)
    I2C 2线        串口2线      串口2线+2线            2线
```

外壳里还要留：**GPS 天线朝上、4G 天线避开金属、NFC 天线贴在你希望手机去碰的那一面。**

---

## 4. 会耗掉多少车电池

按 48V 20Ah（约 960 Wh）算：

| 工作方式 | 待机耗电 | 每天掉 | 停 3 个月掉 |
| --- | --- | --- | --- |
| **4G 常在线**（随时能远程查位置） | 0.5~1.5 mA | 0.06~0.18 % | **约 16 %** |
| **4G 定时开机**（只定时上报，查位置要等） | < 0.2 mA | < 0.024 % | 约 2 % |

**耗电大头是 4G 模组，不是主控芯片。** 主控自己只要 3 µA，可以忽略；
是 4G 模组要不要一直挂在网上，决定了上面这两行的差别。

所以设计上做成两档，平时用省电档。**这也是你要拍板的事之一（§7 第 2 条）。**

⚠ 这里说的是「设备偷偷耗掉的电」，不包括电池自己的自然损耗。
冬天几个月不骑的话，16% 是能感觉到的，2% 感觉不到。

---

## 5. 用起来是什么感觉

**开锁的实际动作：亮屏 → 解锁手机 → 打开 App → 贴车。**

这四步没法减少，是安卓系统的限制，不是没做优化：安卓在**息屏或锁屏时根本不发 NFC 信号**，
而且只允许**前台 App** 使用 NFC 读卡。查过 Android 15/16 的新接口，这两条都没松。

所以不会有「手机在兜里走近就开」这种体验，那需要蓝牙，而蓝牙路线你已经放弃了
（蓝牙要配对、要维持连接、开锁快慢受手机后台调度摆布，反而不稳）。

**iPhone 完全不支持**，这是明确的取舍。

---

## 6. 你已经做的四个取舍，以及代价

这四条都是你主动定的，不是漏掉的。代价写在这里，将来别忘：

| 取舍 | 代价 |
| --- | --- |
| **不装备份电池** | 有人**剪线或拔电池，设备立刻断电死掉**，收不到「被断电」「被拖走」的告警。商用产品（如 Teltonika）都是靠一颗小电池撑这段时间的 |
| **不做硬件欠压保护** | 改成软件判断电压，靠固件降频上报 + 在 HA 报警。极端情况（固件挂了）就没有保护，只能靠电池组自己的 BMS |
| **放弃蓝牙** | 只能贴 NFC 开锁，没有「靠近自动解锁」 |
| **放弃 iPhone** | 家里有 iPhone 的人开不了锁，只能用机械钥匙 |

**机械钥匙必须保留，这不是可选项。**
手机没电、手机丢了、4G 挂了、设备坏了——任何一种都会把你锁在车外面。

⚠ 另外记一笔：**将来如果改主意要加备份电池，必须同时加温度检测。**
0°C 以下给锂电池充电会析锂、有起火风险，这条要求在早前的方案迭代里被误删过一次。

---

## 7. 需要你拍板的事

按急迫程度排。前三条不定，对应的活就没法开工。

**1. 锁用什么？**
电磁锁（通电就开、断电锁上）还是电机锁（转一下）？
这决定了驱动电路、要几根线、以及功耗。**这一项不定，锁相关的元件全都不能买。**
顺带确认车上原来的锁怎么处理——是替换还是并联加一把。

**2. 远程查位置能等多久？**
- 「点一下几秒钟就要看到」→ 4G 要常在线 → 停 3 个月掉 16% 电
- 「等下一次自动上报（比如 5~30 分钟）也行」→ 掉 2% 电，而且电路更简单

**这个问题的答案直接决定上面 §4 那张表走哪一行。** 我的建议是后者：
开锁本来就是离线的，不依赖 4G，那 4G 常在线唯一的用处就是「立刻查位置」，
为这一个功能付 8 倍的电费不太值。

**3. Home Assistant 那台机器和这台机器怎么连？**
同一个内网 / 走 WireGuard / 走公网？
如果要走公网，MQTT 必须加密，否则等于把你所有车的位置公开广播。

**4. ~~要不要换 Air780EPM~~ —— 你已经定了：不换。**
继续用 Air780EP，AT 那一层的代码自己写。代价是最难调的那 2000~4000 行躲不过去，
已经写了一部分（断线重连和睡眠唤醒还缺，见 `FIRMWARE.md`）。

**5. 一件需要你批准的操作。** ~~上报要用的 MQTT 服务器（`/opt/mqtt`）现在缺权限隔离配置。~~
**已不需要**：服务端自己内置了 MQTT broker（因为你只有一辆车），不再动 `/opt/mqtt`。
顺带查到那个 Mosquitto 其实一直没在运行。

---

## 8. 后面按什么顺序做

| 阶段 | 干什么 | 什么时候算过 |
| --- | --- | --- |
| R0~R1 | 环境搭起来；**板子体检测静态电流** | 拿到真实的耗电数字 |
| R2~R3 | 加速度计、NFC 通 | 摇一下有反应；手机贴上去有响应 |
| R4 | 开锁协议 | 录一次开锁再重放，必须开不了 |
| R5 | 服务器 + Home Assistant | 地图上能看到车 |
| R6 | 4G 上报 | 车真的把位置发上来了 |
| R7~R8 | GPS；装供电前端；调两档功耗 | 省电档 < 0.2 mA |
| R9 | 收尾：没信号时用基站定位、欠压报警 | 拔掉 GPS 天线也还能报个大概位置 |

**R1（板子体检）必须排在最前面。** 如果那块板子是 750 µA 的次品，
后面所有省电设计都要重做，早发现省事。

> 详细的每阶段验收标准见 [`DESIGN.md` §10](DESIGN.md)。
